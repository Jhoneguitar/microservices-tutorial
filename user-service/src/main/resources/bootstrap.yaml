spring:
  application:
    name: user-service
  cloud:
    config:
      enable: true
      uri: http://localhost:8081
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/tutorial-realm
