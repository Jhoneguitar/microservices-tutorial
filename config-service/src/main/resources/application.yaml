server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: keycloak
          uri: https://github.com/Jhoneguitar/microservices-tutorial
          search-paths: config-data
  application:
    name: config-service
