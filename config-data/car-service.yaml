server:
  port: 8002
eureka:
  client:
    fetch-registry: true
    register-with-ureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance: 
      hostname: localhost